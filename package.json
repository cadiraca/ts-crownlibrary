{
  "name": "ts-crownlibrary",
  "version": "1.0.0",
  "private": true,
  "description": "CrownLibrary — personal research library (CLI + Web + API + Mobile)",
  "workspaces": [
    "packages/server",
    "packages/cli",
    "packages/web",
    "packages/mobile"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/server && npm run build --workspace=packages/cli && npm run build --workspace=packages/web",
    "dev:server": "npm run dev --workspace=packages/server",
    "dev:web": "npm run dev --workspace=packages/web",
    "start": "npm run start --workspace=packages/server"
  },
  "engines": {
    "node": ">=18"
  }
}
